<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.callor.student.persistance.UserDao">

	<insert id="insert">
		INSERT INTO tbl_users(
			username, password, name, nick, role
		) VALUES (
			#{username},#{password},#{name},#{nick},#{role}
		)
	</insert>

</mapper> 